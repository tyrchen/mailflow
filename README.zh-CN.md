![Build Status](https://github.com/tyrchen/mailflow/workflows/build/badge.svg)

# Mailflow - 无服务器电子邮件分发系统

[English](README.md) | [中文](README.zh-CN.md)

---

一个基于 Rust 和 AWS Lambda 构建的生产就绪、无服务器电子邮件分发系统。Mailflow 允许应用程序通过集中式路由机制接收和响应电子邮件，根据收件人地址提供智能邮件路由。

## 🎯 什么是 Mailflow？

Mailflow 是一个基于 AWS 的电子邮件基础设施，作为您应用程序的邮件网关：

- **入站流程**：通过 Amazon SES 接收邮件 → 路由到应用专用 SQS 队列
- **出站流程**：从 SQS 处理应用响应 → 通过 SES 发送邮件
- **智能路由**：根据收件人模式路由邮件（`_app1@acme.com` → `mailflow-app1` 队列）
- **零运维**：无服务器架构，自动扩展

## ✨ 核心特性

- 🚀 **无服务器**：基于 AWS Lambda 构建，自动扩展，按使用付费
- 💰 **成本高效**：10,000 封邮件/月仅需约 $3（包含所有 AWS 服务）
- 🔒 **安全**：KMS 加密、IAM 最小权限、HTML 清理
- 🎯 **智能路由**：自动将邮件路由到正确的应用队列
- 🔄 **幂等性**：使用 DynamoDB 防止重复发送邮件
- 📊 **可观测**：CloudWatch 日志、指标和告警
- 🏗️ **基础设施即代码**：完整的 Pulumi 部署自动化
- ⚡ **高性能**：处理 50-100 封邮件/分钟，延迟约 2-5 秒
- 🛡️ **可靠**：死信队列、错误处理、至少一次投递保证

## 🏛️ 架构

```
┌─────────────────────────────────────────────────────────────────┐
│                         入站流程                                 │
└─────────────────────────────────────────────────────────────────┘

互联网邮件 → SES → S3 (原始邮件) → Lambda (路由器) → SQS (mailflow-app1)
                      ↓                                  → SQS (mailflow-app2)
                   附件存储                               → SQS (mailflow-appN)

┌─────────────────────────────────────────────────────────────────┐
│                         出站流程                                 │
└─────────────────────────────────────────────────────────────────┘

应用1 → SQS (mailflow-outbound) → Lambda (发送器) → SES → 互联网邮件
应用2 →                ↓
应用N →         幂等性检查
```

**使用的 AWS 服务**：

- **Amazon SES**：邮件接收和发送
- **Amazon S3**：邮件和附件存储
- **Amazon SQS**：消息队列路由
- **AWS Lambda**：无服务器邮件处理（Rust）
- **Amazon DynamoDB**：幂等性追踪
- **Amazon CloudWatch**：日志、指标和告警

## 📋 前置要求

- **Rust**: 1.89+ 和 cargo
- **AWS 账户**：已启用 SES
- **域名**：用于接收邮件（必须在 SES 中验证）
- **Node.js**: 18+（用于 Pulumi 基础设施）
- **Pulumi CLI**：用于基础设施部署
- **cargo-lambda**：用于构建 Lambda 二进制文件

## 🚀 快速开始

### 1. 安装依赖

```bash
# 安装 Rust
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# 安装 cargo-lambda
cargo install cargo-lambda

# 安装 Pulumi
brew install pulumi  # macOS
# 或访问：https://www.pulumi.com/docs/install/

# 安装 Node.js 依赖
cd infra
npm install
```

### 2. 构建 Lambda 二进制文件

```bash
# 为 AWS Lambda (ARM64) 构建
cargo lambda build --release --arm64
```

生成文件：`target/lambda/mailflow/bootstrap.zip`

### 3. 部署基础设施

```bash
cd infra

# 初始化 Pulumi 堆栈
pulumi stack init dev

# 配置
pulumi config set aws:region us-east-1
pulumi config set mailflow:environment dev
pulumi config set mailflow:domains '["yourdomain.com"]'
pulumi config set mailflow:apps '["app1", "app2"]'

# 部署！
pulumi up
```

### 4. 验证 SES 域名

```bash
# 在 SES 中验证域名
aws ses verify-domain-identity --domain yourdomain.com

# 添加 DNS TXT 记录（检查邮件获取验证码）
# 等待验证完成

# 检查状态
aws ses list-verified-email-addresses
```

### 5. 测试

**发送测试邮件到您的应用**：

```bash
aws ses send-email \
  --from test@yourdomain.com \
  --destination ToAddresses=_app1@yourdomain.com \
  --message "Subject={Data=测试邮件},Body={Text={Data=来自 Mailflow 的问候！}}"
```

**检查应用队列**：

```bash
# 获取队列 URL
APP_QUEUE=$(pulumi stack output appQueueUrls -j | jq -r '.app1')

# 接收消息
aws sqs receive-message --queue-url $APP_QUEUE
```

**发送响应邮件**：

```bash
OUTBOUND_QUEUE=$(pulumi stack output outboundQueueUrl)

aws sqs send-message --queue-url $OUTBOUND_QUEUE --message-body '{
  "version": "1.0",
  "correlation_id": "test-'$(date +%s)'",
  "timestamp": "'$(date -u +%Y-%m-%dT%H:%M:%SZ)'",
  "source": "app1",
  "email": {
    "from": {"address": "_app1@yourdomain.com", "name": "App1"},
    "to": [{"address": "recipient@example.com"}],
    "subject": "测试响应",
    "body": {"text": "这是来自 Mailflow 的测试响应"}
  }
}'
```

## 📖 工作原理

### 入站邮件处理

1. **邮件到达**您的域名（例如 `_app1@yourdomain.com`）
2. **SES 接收**邮件并将原始内容保存到 S3
3. **Lambda 触发**并下载邮件
4. **解析器提取**邮件头、正文和元数据
5. **路由器根据收件人确定**目标：
   - `_app1@yourdomain.com` → 路由到 `mailflow-app1` 队列
   - `user@yourdomain.com` → 路由到 `mailflow-default` 队列
6. **消息发送**到 SQS 队列，包含完整的 JSON 格式邮件数据
7. **您的应用**轮询队列并处理邮件

### 出站邮件处理

1. **您的应用**发送 JSON 消息到 `mailflow-outbound` 队列
2. **Lambda 轮询**队列并接收消息
3. **验证器**检查消息架构和必填字段
4. **幂等性**检查防止重复发送
5. **组合器**构建 MIME 邮件（文本、HTML 或多部分）
6. **SES 发送**邮件到收件人
7. **追踪器**在 DynamoDB 中记录关联 ID
8. **队列清理** - 成功发送后删除消息

## 🎨 使用场景

- **多应用邮件网关**：根据收件人将邮件路由到不同应用
- **支持工单系统**：接收支持邮件并路由到工单系统
- **发票处理**：解析发票邮件并路由到会计应用
- **通知系统**：从多个应用发送事务性邮件
- **邮件自动化**：使用路由逻辑构建邮件工作流

## 📊 消息格式

### 入站消息（发送到应用队列）

```json
{
  "version": "1.0",
  "messageId": "mailflow-uuid",
  "timestamp": "2025-10-31T12:34:56Z",
  "source": "mailflow",
  "email": {
    "messageId": "original-email-id",
    "from": {"address": "sender@example.com", "name": "发送者姓名"},
    "to": [{"address": "_app1@yourdomain.com"}],
    "subject": "邮件主题",
    "body": {
      "text": "纯文本正文",
      "html": "<html>HTML 正文</html>"
    },
    "headers": {
      "in-reply-to": "previous-message-id",
      "references": ["msg-1", "msg-2"]
    }
  },
  "metadata": {
    "routingKey": "app1",
    "domain": "yourdomain.com"
  }
}
```

### 出站消息（发送到 mailflow-outbound 队列）

```json
{
  "version": "1.0",
  "correlationId": "unique-id-123",
  "timestamp": "2025-10-31T12:34:56Z",
  "source": "app1",
  "email": {
    "from": {"address": "_app1@yourdomain.com", "name": "App1"},
    "to": [{"address": "recipient@example.com"}],
    "subject": "响应邮件",
    "body": {
      "text": "纯文本响应",
      "html": "<p>HTML 响应</p>"
    }
  }
}
```

## 🔧 配置

所有配置通过 Pulumi 管理。添加新应用：

```bash
# 更新应用列表
pulumi config set mailflow:apps '["app1", "app2", "newapp"]'

# 部署
pulumi up
```

自动完成：

- 创建 `mailflow-newapp-dev` SQS 队列
- 更新 Lambda 路由映射
- 更新 IAM 权限

**总用时：约 2 分钟，零代码更改！**

## 📈 监控

**CloudWatch 告警**：

- Lambda 错误（5 分钟内 >10 次）
- DLQ 有消息（>0）
- 处理缓慢（平均 >30 秒）

**查看日志**：

```bash
aws logs tail /aws/lambda/mailflow-dev --follow
```

**检查队列深度**：

```bash
aws sqs get-queue-attributes \
  --queue-url $(pulumi stack output outboundQueueUrl) \
  --attribute-names ApproximateNumberOfMessages
```

## 🧪 开发

```bash
# 运行测试
make test

# 格式化代码
make fmt-fix

# 代码检查
make lint

# 完整检查（格式 + 检查 + 测试）
make check

# 构建本地测试版本
make build

# 构建 Lambda 版本
make lambda-build
```

## 📚 文档

- **[产品规格](specs/0001-spec.md)**：详细需求和功能
- **[设计规格](specs/0002-design.md)**：架构和技术设计
- **[实施计划](specs/0003-implementation-plan.md)**：开发路线图
- **[评审报告](REVIEW.md)**：差距分析和合规性检查
- **[实施状态](STATUS.md)**：实施进展
- **[基础设施指南](infra/README.md)**：完整设置说明

## 💰 成本分析

**10,000 封邮件/月的成本**：

- Lambda：$0.10
- S3：$0.30
- SQS：$0.02
- DynamoDB：$1.25
- SES：$1.00
- CloudWatch：$0.50

**总计：约 $3.27/月** 💸

## 🎯 实现状态

| 阶段            | 状态   | 测试    |
|---------------|------|---------|
| 阶段 0：项目设置 | ✅ 完成 | N/A     |
| 阶段 1：基础     | ✅ 完成 | 21 通过 |
| 阶段 2：入站处理 | ✅ 完成 | 25 通过 |
| 阶段 3：出站处理 | ✅ 完成 | 28 通过 |
| 阶段 4：基础设施 | ✅ 完成 | N/A     |

**总计：28 个测试通过，构建成功，核心功能完整！**

## 💡 技术亮点

- **Rust 实现**：内存安全、零成本抽象、高性能
- **Trait 基础设计**：可测试、可扩展的架构
- **类型安全**：编译时保证正确性
- **异步处理**：Tokio 运行时，高并发
- **邮件库**：
  - `mail-parser`：RFC 兼容的邮件解析
  - `lettre`：功能完整的 MIME 邮件组合

## 🔍 故障排查

**Lambda 未被邮件触发**：

- 检查 SES 域名已验证
- 检查 SES 接收规则已激活
- 检查 Lambda 有 SES 调用权限

**队列未收到消息**：

- 检查 Lambda 日志查看错误
- 验证邮件发送到正确地址模式：`_<app>@<domain>`
- 检查路由配置

**DLQ 中有消息**：

- 查看消息内容了解错误
- 检查 Lambda 错误日志
- 必要时手动重新处理

## 🤝 贡献

1. Fork 本仓库
2. 创建功能分支
3. 进行更改
4. 运行测试：`make check`
5. 提交 Pull Request

## 📄 许可证

本项目采用 MIT 许可证分发。

详见 [LICENSE](LICENSE.md)。

版权所有 2025 Tyr Chen
